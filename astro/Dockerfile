# Astro Runtime 3.x is Astronomer's Apache Airflow 3.x distribution.
# The pinned tag below matches the default target shown in the
# `calibration-check` deployment created on 2026-04-25; bump in lockstep
# with the deployment's "DOCKER IMAGE target" field in the Astro UI.
FROM quay.io/astronomer/astro-runtime:3.2-2
